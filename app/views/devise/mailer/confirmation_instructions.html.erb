<p>Welcome <%= @resource.first_name %> <%= @resource.last_name %>! To Feedit. </p>

<p>Before you start using Feedit,you need to confirm your account email through the link below:</p>

<p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token) %></p>
